*.pyc
__pycache__/
*.log
.DS_Store
.pytest_cache/
.coverage

# notebook-display PNG sidecars regenerated by make demo-notebook
tercile_spread.png
delta_severity_spread.png

# Third-party data fetched at runtime by each quantlet's fetch_data.py
# (Yahoo prices, Ken French factors). Never committed; first `make data`
# re-downloads locally.
*/prices.csv
*/ff_factors.csv

# Loughran-McDonald master dictionary IS committed alongside quantlet 02
# (free for academic use per SRAF policy). Anyone fetching the dictionary
# at runtime via data.py would get the same upstream CSV.

# CCRS internal state (Claude Code runtime — never tracked)
.ccrs/
